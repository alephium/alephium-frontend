{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["ts:check", "compile", "^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "lint:fix": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "ts:check": {},
    "test": {},
    "compile": {
      "dependsOn": ["clean"],
      "outputs": ["dist/**"]
    },
    "clean": {}
  }
}
