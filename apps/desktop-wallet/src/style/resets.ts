import { css } from 'styled-components'

export default css`
  * {
    box-sizing: border-box;
  }

  input,
  button {
    outline: none;
    border: none;
    background: transparent;
  }

  a {
    text-decoration: none;
    color: inherit;
  }
`
