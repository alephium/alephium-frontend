// import { FloatingDevTools } from '@buoy-gg/core'

// Enable only when you need to debug, otherwise it gets added to the production bundle
const Devtools = () => null // <FloatingDevTools />

export default Devtools
